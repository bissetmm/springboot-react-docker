#!/bin/sh
# ----------------------------------------------------------------------------
# Maven Wrapper Startup Script
# ----------------------------------------------------------------------------

MVN_EXEC="mvn"
if [ -x "$DIR/mvnw.cmd" ]; then
  MVN_EXEC="$DIR/mvnw.cmd"
fi

exec "$MVN_EXEC" "$@"
